package com.example.www.popularmovie;

import org.junit.Test;

/**
 * Created by mo on 24.02.18.
 */

public class RunningFile {
    @Test
    public void prepareAsync() {
        /*MovieAsyncTask t = new MovieAsyncTask();
        try {
            t.execute(MovieConnectionUtils.buildMovieUrl(TOP_RATED, null));
            while(t.getStatus() != AsyncTask.Status.FINISHED){
                sleep(5000);
            }

        } catch (Exception e) {
            Log.d("RunningFile", e.getMessage());
        }*/
    }
}
